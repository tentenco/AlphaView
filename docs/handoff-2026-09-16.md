# Claude Code 接手分析（2026-09-16）

> 本文保留當日較早的接手快照，檔案數、服務狀態及待辦文字可能已過時。Claude Code → Codex 的後續交接與程式核對結果見 [Codex 接手文件](codex-handoff-2026-09-16.md)。目前 Git 狀態以現場指令為準。

本文整理 Codex Agent 在此專案的三輪工作、目前工作樹的實際狀態、已驗證的證據，以及接手後的建議順序。它是接手當下的快照；之後的狀態以 `git status`、測試與 `artifacts/` 內的 receipts 為準。文中不含任何實際持股股數或成本。

## 一、Codex 做過什麼

| 時段（臺灣時間） | 形式 | 交付 | 證據 |
| --- | --- | --- | --- |
| 09-05 06:47–11:59 | 5 小時 Harness、多子代理（產品／可靠性／前端／指標驗證） | 品牌改名 AlphaView、README、競品對標、250／500／1,000 股票池、資料品質與 provenance、回測 v5、備份與預檢、排程、儲存清理、標的比較、長測與 IPC 修正。共 64 項完成、9 項限制 | `artifacts/harness-2026-09-05/state.json`、`review.html`、commits `5fd6de2`…`679dd91` |
| 09-07 00:52–03:52 | 3 小時 Harness、單線實作 | Alpha Picks 首頁、Alpha 實驗室（回放、組合實驗、實驗筆記本）、持倉提醒、研究進度、比較收藏、權重方案、桌面提醒、研究備份、ICS、交接包。共 33 項完成 | `artifacts/harness-2026-09-07/state.json`、`alpha-review.html`、`source-checkpoint.zip`（77 檔） |
| 09-15 12:12–12:37 | 互動修復（非 Harness） | 發現 `data/panel.db` 為空庫 → 以 09-05 備份還原並核對 → 依使用者截圖更新持股 → 修正 yfinance 新版 `HistoryMetadata` 被誤判為格式錯誤 → 全站字級放大（16px 內文） | `artifacts/recovery-20260915T041829Z/recovery-report.json`、`position-update-…/refresh-fix-verification.json`、`typography-…/verification.json` |

Codex 的全域 `~/.codex/AGENTS.md` 只有任務排隊與部落格路由政策，與本專案無關；專案內原本**沒有** AGENTS.md／CLAUDE.md。Codex 的專案知識散在 `docs/`、兩份 `state.json` 與對話紀錄，本次已整理進 `CLAUDE.md`。

## 二、工作樹狀態

HEAD 為 `679dd91`（09-05 最後一筆 docs commit，分支 `main`，遠端 `tentenco/AlphaView`）。09-07 與 09-15 的所有變更都**尚未提交**，共 80 個路徑（15 個修改、65 個新增），可分成三組：

0. **Claude Code 09-16 新增**：`CLAUDE.md`、`AGENTS.md`、本文件；市場風險溫度計（`alphaview/panel/market_regime.py`、`tests/test_market_regime.py`、`web/src/{MarketRegime.tsx,MarketRegime.test.tsx,market-regime.ts,market-regime.test.ts,regime.css}`、`MarketOverview.tsx` 與 `App.tsx` 掛載、`docs/market-regime.md`、README／WEB_PANEL 段落）。可獨立成第四個 commit（`feat: market risk temperature from crash-monitor factors`）。
1. **Alpha 工作流（09-06～07）**：`alphaview/panel/{alpha_replay,alpha_basket,holding_fit}.py` 與對應測試；`web/src/Alpha*.tsx`、`alpha-*.ts`、`research-*.ts`、`weight-profiles.ts`、`comparison-bookmarks.ts`、`locale.tsx`、`theme.ts` 等；`docs/alpha-*.md`；`scripts/{render_alpha_review.py,alpha_review_client.js,checkpoint_harness.py}`；`api.py` 註冊三個 router；`jobs.py` 新增 `scan_all`。
2. **yfinance 相容修正（09-15）**：`market.py` 以 `collections.abc.Mapping` 接受 lazy metadata（避免觸發盤中 tradingPeriods 請求）；`tests/test_universe_metadata_validation.py` 以 `HistoryMetadata` 包裝驗證。這是**行情更新恢復正常的必要修正**，優先保留。
3. **字級與版面（09-15）**：`styles.css`（640 行差異，改為 `--type-*` rem tokens）、`alpha.css`、`comparison.css`、`Charts.tsx`、`ComparisonChart.tsx`、`AlphaBasket.tsx`、`AlphaOpportunityMap.tsx`。改前檔案備份在 `artifacts/typography-20260915T042925Z/`。

09-16 接手時本機驗證：pytest **519 通過**、vitest **218 通過／41 檔**。加入市場風險溫度計後為 pytest **575**、vitest **226／43 檔**，prettier、`npm run build` 與 `git diff --check` 皆通過；`web/dist` 已重新建置。

**建議接手第一步是分三個 commit 提交上述三組**（訊息可用 `feat: Alpha Picks research workflow and lab`、`fix: accept lazy yfinance history metadata`、`style: rem type scale and larger body text`），再開始新工作。是否提交由使用者決定；未取得同意前不要動 git 狀態。

## 三、資料與環境

- 工作區 `data/panel.db`（約 500 MB）：09-15 從 `artifacts/harness-2026-09-05/private-backup-check/alphaview-backup-2026-09-05T02-39-10.zip` 還原，接著更新持股並成功更新 7 檔至 09-14 收盤。09-09 建立的空庫另存 `artifacts/recovery-…/empty-before-restore.db`。**為何 09-09 會出現空庫仍未查明**；可能方向：另一個程序以不同 cwd 啟動 `serve`（`store.db_path()` 以 `PANEL_DB_PATH` 或 repo 相對路徑決定）、或外部清理。接手時先確認 `positions` 有資料再做任何寫入。
- `data/sequoia_v2.db` 是舊 A 股資料庫，Web 面板不使用。
- 市場池上次完整探索為 999 檔（09-05），其中 13 檔日線異常、84 檔缺行情；SPY 最新日線曾非有限值；APH 的 yfinance repair 大量改寫歷史價（見 `docs/data-provider-evaluation.md`）。這些是資料狀態，不是程式錯誤。
- `.env` 只服務 A 股 CLI；Web 面板不讀它。`PANEL_DB_PATH` 可指向隔離工作區。
- 目前沒有伺服器在跑（8876／8878 皆無 listener）。啟動：`.venv/bin/python -m alphaview.panel serve`。

## 四、Codex 的方法論（接手必須沿用）

已完整寫入 `CLAUDE.md`「不可違反的原則」。摘要：不捏造資料；每種計算有版本；讀寫一致與原子發布；缺資料不重分配權重；研究不是交易；私人資料不進 git／文件；衝突保護與草稿保留；交易日以 XNYS 收盤後 15 分鐘為準；不做外部動作；commit 由使用者決定。

Codex 的產出風格值得沿用：每項功能都附「這不是什麼」的邊界說明、每次 Harness 都留下可離線開啟的 HTML 檢閱頁與 JSON 匯出、每個聲稱都對應一份 receipt。

## 五、待辦優先序（合併兩輪 `next_tasks` 與使用者 09-16 指示）

| 順序 | 項目 | 來源與說明 |
| --- | --- | --- |
| 0 | 提交三組未提交變更（見第二節） | 讓後續工作有乾淨基線 |
| 1 | **市場風險溫度計**（本次已交付第一版，見 `docs/market-regime.md`） | 使用者 09-16 指示整合 US_Stock_Crash_Monitor 方法論 |
| 2 | 隔離的 ZIP 恢復工作區（P1-A/B/C） | `docs/next-harness-workspace.md`；09-15 的手動還原證明此需求真實 |
| 3 | 原始／修復行情版本 review 與有限重試 | `docs/next-harness-data.md` P0／P1 |
| 4 | 經驗證的 SPY／VOO 市場基準 | `docs/next-harness-data.md`；溫度計的技術因子也依賴同一份基準日線 |
| 5 | 財報／事件行事曆 | 09-07 `next_tasks` P1；需新資料源與授權評估 |
| 6 | 交易／現金流帳本 | `docs/next-harness-methodology.md`，含可手算驗收案例 |
| 7 | 研究進度／偏好的工作區同步、排程日報、可組合策略規則 | 09-07 `next_tasks` P2／P3 |

Vite 主 chunk 大小、Harness STOP 延遲（09-05 晚 11 分 41 秒寫入）也在待辦清單，屬工程債。

## 六、風險與注意事項

- 未提交變更橫跨兩次 session，`styles.css` 差異很大；若要局部回退字級，用 `artifacts/typography-…/` 內的備份逐檔 diff，不要整檔 checkout（會連 09-07 的樣式一起丟掉）。
- `locale.tsx` 以 DOM 文字替換做翻譯，翻譯表按字串長度排序後逐一 `split/join`；新增中文字串若是既有字串的子字串可能被提前替換，寫完要在英文介面實際檢查。
- 測試對 `sessions.latest_completed_session` 多以 monkeypatch 固定日期；新測試也要固定，否則週末／假日會飄。
- `artifacts/` 內有真實持股與備份，任何報告產生器都不得把它們的內容嵌進可分享的檔案。
