# AlphaView Web Panel

繁體中文、單一使用者、本地運行的美股研究面板。視覺參照 Tenten CRM：
窄版導覽、Geist 字體、深色中性底色、`#006b4f` 主操作色、5px 控制項圓角。
此面板專注美股研究，資料儲存在本地。

## 啟動

```sh
uv sync --locked --extra web --extra dev
npm ci --prefix web
npm run build --prefix web
uv run --extra web python -m alphaview.panel serve
```

開啟 **http://127.0.0.1:8876**。預設僅綁定本機，請從專案目錄啟動。
這是個人工作區，尚未提供公開部署所需的登入、多租戶與權限管理。

前端開發使用 `npm run dev --prefix web`（5173），後端照上方啟動。
API 文件：http://127.0.0.1:8876/docs 。

## 初始化與資料

可選擇建立通用的零持股觀察清單：

```sh
uv run --extra web python -m alphaview.panel seed
uv run --extra web python -m alphaview.panel refresh
```

`seed` 不覆寫既有持股，也不包含任何個人股數、成本或帳戶資料。
初始觀察清單為 AAPL、MSFT、GOOGL、AMZN、META、NVDA、TSLA，全部為零股。
你可以在「我的持股」輸入自己的持股與平均成本；既有本機工作區會繼續保留資料。

- 行情：Yahoo Finance / yfinance，最近兩年日線；僅接受 USD。
- SPCX 核對為 SpaceX，只接受名稱含 SpaceX / Space Exploration 的資料，
  並排除 2026-06-12 前的資料，避免誤用同代碼舊 ETF。
- 資料庫：`data/panel.db`；可用 `PANEL_DB_PATH` 指向其他本地路徑。
- 策略、圖表：使用含股息調整日線。持股估值：使用日線收盤價。
- 持股盈虧：股數 ×（收盤價 − 匯入平均成本）。不含交易費、稅與已實現損益。
- 當日盈虧：目前股數 ×（本次與前次日線收盤價之差），不含股息現金流；
  假設兩日持股數不變，可能與券商的調整前收盤、即時/盤後或公司行動口徑不同。
- 市值與當日盈虧僅使用行情資料，不以使用者手動資料作為價格備援。最新收盤價無效時留空，
  不改用較舊價格假裝是最新報價。總覽會標明估值涵蓋檔數；部分缺值時市值／未實現盈虧可能僅為可用部分小計。
- 過期報價保留原日期與估值，但標示過期，當日漲跌不計算；總覽分開顯示可估值與當期報價涵蓋。
  未收盤或未來日期的最新快取不參與估值。歷史個股查詢以指定日期對應的已完成交易日判斷新鮮度，休市日對齊前一交易日。
- 單檔日漲跌須有相鄰交易日的有效收盤價；前後跨缺日、價格無效或計算溢位時留空。
  組合日盈虧若涵蓋不足或報價日期混用，顯示不可用與涵蓋檔數，不把缺失標的當成零漲跌。
- 圖表保留缺少交易日或無效觀測的斷點，不以直線跨接或補造價格。個股明細會說明資料缺口。
  歷史日期的股數與成本仍是目前設定，不表示當時真的持有這些數量；歷史權重不顯示。
- 暫不追蹤交易流水，因此持股變動、股數拆分與成本調整需要使用者自行編輯。
- 依交易日曆確認收盤並等待 15 分鐘，避免把尚未完成的交易日當作收盤。
  提早收市、休市與夏令時間依 XNYS 日曆處理。
- 更新失敗保留該標的先前行情，資料管理頁標示錯誤；日線日期不同時不混入同日選股。

來源核對：
- [yfinance 官方文件](https://ranaroussi.github.io/yfinance/)
- [Cboe SPCU 標的說明（SpaceX / SPCX）](https://www.cboe.com/us/equities/listings/listed_products/symbols/SPCU/)
- [SPCX 舊 ETF 改名 SPCK 的 SEC 公告](https://www.sec.gov/Archives/edgar/data/1719812/000199937126007611/cist-497_040226.htm)

## 策略與回測

四種日線條件：海龜突破、均線趨勢、RSI 回檔觀察、清單內相對強勢。
完整條件可在策略研究頁查看。

相對強勢只比較**所選股票池**，不代表全美股排名。歷史選股使用該次掃描的清單，
屬於研究重算，不是當時實際持倉或當時全市場的可交易投資組合。
每次掃描儲存最近 60 個交易日的結果，可切換日期查閱。

回測結果也會存入本地資料庫；切換標的與策略時載入最近一次結果，頁面顯示計算時間與期間。行情更新後可再按「執行回測」重新計算。

三種單檔回測：前日收盤訊號、次日開盤成交；全額做多，每次買賣按成交金額預設扣 0.1% 成本（可設 0–100 bps）。
買入成交金額＝可用資金／（1＋成本率），買入費用為資金與成交金額之差；賣出成本＝賣出成交金額 × 成本率。
費用與滑價合併估計，不另調整成交價格。
初始資金預設 $10,000，可自行設定；指標暖機後開始；不同策略的起始日可能不同，應連同日期比較。

- 海龜退出：前日收盤低於之前 10 日最低價。
- 趨勢退出：前日收盤低於 MA50。
- 回檔退出：前日 RSI ≥ 60 或收盤低於 MA200。
- 末日未平倉按收盤評價，沒有假設末日強制賣出費用。
- 基準為同起始日買入持有，未扣費，使用同一調整價格序列。
- 不模擬融資、借券、稅、流動性限制與盤中停損。
- 資料不足會拒絕回測；不補造數值。

此工具產生研究訊號與模擬結果，沒有券商連線、自動下單或 Feishu 推送。

## 市場選股與股票池上限

「每日選股」預設使用獨立的**美股市場候選股票池**，不再只掃描持股。
先在「股票池上限」選擇 **250、500 或 1,000 檔**，再按「執行市場選股」，
才會更新候選清單、下載日線、重新計算四個策略。只切換上限不會立即改寫現有股票池。
畫面分開顯示本次設定上限與目前實際清單；上限是目標最大值，不保證一定取得同樣數量。

股票池由 Yahoo Equity Screener 即時查詢建立：

- `region=us`，Nasdaq NMS / NYSE NYQ，美元普通股類型（含 ADR）。
- 市值至少 20 億美元、股價至少 5 美元、近三個月平均日成交量至少 20 萬股。
- 按市值遞減分頁擷取，最多取所選的 250／500／1,000 筆候選。不同股別可能各占一檔；
  這不是完整全美股或指數成分。通過股票身分驗證、去重後的實際檔數可能較少。
- 每次請求最多 250 筆，依 `offset` 順序取得各頁；資料源總數不足時依實際總數結束。
  分頁資料並非同一瞬間的原子市場快照，不能保證是跨頁期間完全不變的市值排名。
- 市場清單存於 `market_universe`，與 `positions` 分開；`market_universe_metadata` 記錄
  目標上限、來源總數、原始筆數、驗證後檔數、頁數與取得時間。清單與 metadata 一起發布。
- 市場與持股掃描有各自的 `scope`、歷史結果與相對強勢排名。
- 必要分頁全部完成後才取代股票池。頁面失敗、頁數資訊不符、重複頁或發布前取消，
  均保留原股票池；不拿第一頁冒充完整結果，也不退回個人持股清單。

預設只顯示符合至少一種條件、尚未加入持股／觀察名單的新標的。
可搜尋公司、按策略與日期篩選、翻頁、打開該日期的個股明細；「加入觀察」
以零股與空成本加入追蹤，不覆寫既有持股。查詢市場不會自動加入任何持股。

歷史市場訊號以當次取得的股票池重算，並非當時真實成分，存在選樣與存活者偏差。
相對強度只對當次同日有效股票池排名，不能直接與個人清單的排名互相比較。
擴大股票池或改變成分，即使某檔股價完全不變，也可能改變它的 RPS；解讀異動時需一起查看股票池。

CLI 亦支援：

```sh
# 更新最多 500 檔，也可指定 250 或 1000
uv run --extra web python -m alphaview.panel refresh --scope market --universe-limit 500
# 只用目前股票池與已下載日線重算，不重建或縮小股票池
uv run --extra web python -m alphaview.panel scan --scope market
```

`--universe-limit` 僅適用於 `refresh --scope market`，省略時 CLI 預設 250。
API 的市場更新作業接受相同 `universe_limit` 選項；指定標的重試不重建股票池，也不將擴大的清單縮小。

資料管理同時顯示市場候選與自己的清單，提供搜尋與分頁。
[Yahoo / yfinance Screener 查詢文件](https://ranaroussi.github.io/yfinance/reference/api/yfinance.screen.html)

## 日常使用

總覽的「更新行情」下載持股清單並自動掃描；總覽的「執行選股」用已儲存行情重算。每日選股頁的市場／清單選股操作則會先下載相應範圍的行情。
持股可新增、修改或把股數設為 0 移入觀察名單，亦可匯出 CSV。
可手動更新，亦可在資料管理啟用本地收盤排程（預設停用）。同一工作區不允許同時執行兩個資料作業。
CLI 與面板共用工作區鎖；另一個寫入作業執行時會拒絕啟動，請等它完成或取消後再執行。

### 本地收盤排程

在資料管理選擇市場候選或自己的清單、市場上限 250／500／1,000，再勾選啟用並儲存。排程預設停用，必須讓本地伺服器持續運行且電腦保持喚醒；它不是雲端排程服務。每約 60 秒檢查一次，依 XNYS 交易日曆找出正式收盤至少 15 分鐘後的最新交易日，包含假日與提早收盤安排。

- 每個工作區、每個符合時間條件的交易日最多自動嘗試一次；部分完成、失敗、取消或中斷都算一次，不會無限自動重試。
- 休眠後喚醒或重啟只補最新符合條件的交易日，不逐日補跑所有遺漏日期。其他作業佔用工作區時，尚未取得執行權的排程會等後續檢查。
- 失敗後可在資料管理手動重試指定標的，或手動更新行情。更換排程股票池不會讓已嘗試的交易日再自動跑一次。
- 停用只阻止後續啟動，不取消執行中的作業；要停止下載，請另外使用作業取消按鈕。
- 頁面顯示最新符合條件的交易日、下次檢查目標及最近一次自動嘗試。設定有版本檢查，多視窗衝突時須重新載入後確認。

API：`GET /api/schedule` 讀取設定與狀態；`PUT /api/schedule` 傳入 `enabled`、`scope`、`universe_limit` 與目前 `version` 儲存，版本衝突回傳 409。排程狀態不保證行情供應者當時已完整提供日線，仍須查看作業結果與資料品質。

## 驗證

```sh
uv run --extra web --extra dev pytest -q
npm test --prefix web
npm run format:check --prefix web
npm run build --prefix web
```

測試包含歷史日期不讀取未來 bar、次日開盤成交與成本、資料不足、
資料源失敗保留舊資料、持股估值涵蓋率、圖表斷點、輸入驗證、CSV 公式轉義與跨站寫入限制。
分頁股票池亦涵蓋 250／500／1,000 上限、合法短頁、重複頁、失敗回滾、取消及重試不縮池測試；
這些是隔離的模擬資料測試，不等於宣稱每個上限都已完成真實市場全量下載。

設計技能：`ui-design` 的 Build 模式；讀取 `design-guidelines.md`，並套用
colors、dashboards、tables、surfaces、navigation、typography、buttons、
form-controls、responsive-design、flexbox-layout、border-radius、icons、general、
headers、shadows、badges、svg、custom-fonts、dark-mode 規則；字體與色彩以 CRM 參照優先。

## AlphaView 研究工作流程

### 儲存篩選與匯出

每日選股的「進階篩選與儲存設定」可指定調整股價、RSI、相對成交量、RPS 與符合策略數。
缺值不會通過對應數值條件。條件組合以版本化資料儲存在目前瀏覽器，可命名、更新及刪除；
切換股票池時使用該池最新日期，不把先前股票池的日期混用。CSV 包含所有符合條件的分頁、
計算日期、股票池及訊號原因，文字欄位做公式注入防護。

### 持股 CSV 預覽與合併匯入

在「我的持股」開啟「匯入 CSV」，貼上或選擇檔案，先預覽新增、修改與未變更的標的，
再確認整批匯入。資料只寫入本機工作區；未出現在 CSV 的既有持股會保留，不會刪除。
匯入不下載行情，也不把檔案裡的報價或損益當成可信行情。

- 必要標頭：`symbol`、`shares`、`cost`；可選標頭：`name`、`sector`。
- 代碼會轉成大寫；重複代碼不分大小寫，整批拒絕。
- 股數與成本使用小數點，不含貨幣符號或千分位逗號，範圍為 0–1,000,000,000。
  無限值、NaN、負數或無法可靠表示的極小數會拒絕。
- 股數大於零必須填成本；零股觀察標的的成本可留空。
- 缺少可選標頭時，保留既有名稱／產業；新標的預設以代碼為名稱、產業為「自訂清單」。
  明確提供的名稱須為 1–80 字元，產業最多 50 字元。
- 每次最多 100 列，與既有清單合併後也不得超過 100 檔。
- 回匯現有匯出檔時，`price`、`market_value`、`pnl` 等計算或額外欄位會忽略，預覽會提示。

以下三列僅示範格式，股數與成本都是**虛構範例**，並非目前行情或實際持股：

```csv
symbol,shares,cost,name,sector
AAPL,2,100,Apple,Technology
MSFT,1.5,200,Microsoft,Technology
NVDA,0,,NVIDIA,Technology
```

任何一列驗證失敗都不會部分匯入。預覽會綁定目前完整持股清單及標準化輸入的指紋；
若另一個視窗編輯持股，或預覽後變更 CSV，確認時會回傳衝突（409），需重新預覽。
確認匯入與其他資料作業共用工作區鎖，並在單一資料庫交易內重驗與合併；儲存失敗整批回復。

### 每日訊號異動

比較同股票池、所選日期與嚴格早於它的前一個已儲存日期。相同日期重跑不算下一交易日。
新符合／退出僅在兩側資料皆可判斷時成立；缺資料、過期、歷史不足或異常列為無法比較。
股票池新增／移除另列。歷史重算使用當次候選名單，並非完整的當時成分股歷史。

### 本機研究筆記

個股明細可儲存最多 6,000 字元的筆記與 5 個標籤。筆記以版本號防止不同視窗靜默覆寫；
遇到衝突保留草稿，需自行重新載入／合併。關閉有未儲存筆記的明細時會提示確認捨棄。
筆記不會自動產生投資結論，也不傳送到外部服務。

### 資料品質與重試

使用 XNYS 交易日曆辨識美股休市、夏令時間及提早收盤；收盤後等待 15 分鐘才接受當日日線。
檢查有限正價格、OHLC 關係、非負成交量、日期唯一性與已上市資料區間內缺少的交易日。
零成交量但其他欄位有效的日線保留，不任意移除日期或補造價格。資料品質檢查會顯示缺口與
無效日期；異常歷史不產生策略訊號，回測會拒絕而非默默跨過缺口。

可選取最多 100 檔重試，不重新建立市場名單。API 與 CLI 使用同一工作區的跨程序鎖，
避免同時寫入。同一日期批次的全部選股結果以單一交易發布；計算或寫入失敗不會留下半份結果。
取消作業會停止後續下載，等待已開始的請求結束；已完成下載及完整發布的紀錄保留。
取消與發布以資料庫交易排序，未完成的批次不發布。

### 回測診斷

可以設定起始資金、每邊成本（0–100 bps）及日期範圍。日期範圍外的前置資料只用於暖機，
不產生起日前的交易；結束日之後的資料不參與計算或快取指紋。報告顯示實際起訖日期。

- CAGR：以首末交易日之間的日曆天數 / 365.25 年化。
- 波動率與 Sharpe：每日淨值報酬、樣本標準差、252 日年化；無風險利率設為零。
- 勝率／獲利因子／平均持有天數：僅使用已平倉交易；無法定義時留空。
- 曝險率：收盤仍有部位的交易日比例。
- 小樣本與短期間會顯示限制，不給予無依據的可靠度評分。
- API 保留完整金額精度，UI 才格式化顯示，避免極小資金被捨入成錯誤報酬。
- 儲存結果依標的、策略、資金、成本、日期查找；行情或計算引擎改變會提示需重算。

[交易日曆套件](https://github.com/gerrymanoim/exchange_calendars) ·
[Python 工作區檔案鎖](https://docs.python.org/3/library/fcntl.html)


### 下載本機備份

資料管理提供使用者主動觸發的 ZIP 備份，內容如下：

| 檔案 | 內容 |
| --- | --- |
| `alphaview.db` | 持股、成本、已儲存筆記、行情、股票池、選股快照、回測與作業紀錄 |
| `research-notes.json` | 從同一份備份資料庫讀出的筆記與版本，方便另行查看 |
| `browser-settings.json` | 這個瀏覽器提交的有效命名篩選設定與股票池上限 |
| `manifest.json` | 格式／程式／引擎版本、快照時間、schema 指紋、資料表筆數及檔案 SHA-256 |

備份使用 SQLite 的線上 backup API，先固定唯讀快照，再分頁複製；包含尚在 WAL 中
但已提交的資料，不直接複製使用中的 `.db` 檔。持股、筆記與其他資料表來自同一個一致
時間點；備份期間正常寫入可繼續。建立過程有時間上限，一次只準備一份備份，完成下載
或建立失敗後清理專案忽略目錄裡的暫存檔。

下載檔**未加密**，包含個人持股與筆記，請依自己的保存方式妥善存放。未儲存的筆記
草稿、程式碼、環境變數與不相關的瀏覽器資料不在備份內。執行中的作業會以快照當時
的狀態保留，並不表示它已完成。

此功能只下載、不自動還原，也不覆寫目前工作區。不要在伺服器運作期間以檔案複製
替換資料庫；還原需要另外驗證備份及確認操作。持股 CSV 匯入只合併持股欄位，無法
取代整個工作區備份。

[SQLite 線上備份說明](https://www.sqlite.org/backup.html) ·
[Python 3.12 backup API](https://docs.python.org/3.12/library/sqlite3.html#sqlite3.Connection.backup)


### 持股集中度與相關性

持股頁提供 60／120 個交易日的診斷，只分析目前股數大於零的標的，最多 100 檔。這是目前持股的集中度與歷史價格連動，不是歷史組合績效；不假設過去已持有目前股數。

- 估值採最新已完成 XNYS 交易日的有效未調整收盤價。只要有一檔持股缺少有效估值，就不顯示權重、最大持股占比或前三大占比；有效金額另標為小計並顯示涵蓋數。
- 相關性採調整收盤價的相鄰交易日日報酬，每對至少需要 40 筆共同有效觀測。缺日或無效價格不補值，也不把跨多日漲跌當成單日報酬。
- 最新行情過期的標的不納入；零波動、樣本不足或數值不可用顯示原因。各配對顯示自己的共同日期範圍與觀測數。
- 配對可能使用不同日期，不應直接當成完整共變異數矩陣或 VaR，也不代表未來風險預測。API 為 `GET /api/portfolio/risk?window=60`（亦可選 120），只讀取本地資料，不啟動下載。

目前回測引擎為 `alphaview-backtest-v5`，新增策略必要指標的有限數值檢查。舊版本已儲存報告仍可查閱，快取指紋會提示需重算；成交金額計費方式維持前述規則。


### 儲存用量與舊快照清理

資料管理先顯示預覽：資料表筆數、所列文字欄位的 UTF-8 位元組數、資料庫與 WAL 實際檔案大小、SQLite 可重用頁面。文字內容容量不含索引及資料頁開銷，不等於資料表總磁碟用量；其他作業提交時，檔案量測值可能改變。

清理必須先取得最新預覽，再由使用者明確確認。僅刪除同一股票池、同一日期被較新版本取代的選股快照，每個 `(scope, date)` 都保留最新 ID。持股、筆記、行情、回測、作業與排程嘗試紀錄全部保留。可先使用本地備份功能下載 ZIP；系統不自動下載或清理。

API：`GET /api/storage` 取得用量與指紋；`POST /api/storage/cleanup` 傳入 `expected_fingerprint` 與 `confirm: true`。預覽後快照集合有變，或另一資料作業執行中，回傳 409 並要求重新預覽。刪除在單一交易內完成，失敗會回復，不執行 `VACUUM`；空間供 SQLite 重用，檔案不一定立即縮小。

### 持股編輯衝突

網頁編輯器儲存時傳入讀取時的 `expected_updated_at`，若其他視窗或匯入作業已修改該標的，回傳 409。請關閉並重新開啟編輯器，核對最新資料後再儲存；新增標的傳入 null，也會偵測同時新增。舊 API 客戶端若省略此欄位，維持後寫入覆蓋的相容行為，不具有這項衝突保護。

外部 SPY 基準目前尚未實作；現有回測基準仍是同一標的買入持有。導入 SPY 需要另外驗證其調整行情、共同日期與缺值狀態，不能由單次資料檢查推論所有日期都可用或都缺值。

總覽、個股、資料品質與選股查詢使用一致的唯讀資料快照；行情更新或清理同時提交時，不會把提交前後的資料混成一次回應。

## 標的比較

從左側「標的比較」選擇個人清單或市場候選池中的 2–5 檔股票，設定 60、120 或 252 個交易日，按「比較調整收盤價」。這是唯讀計算，不下載行情。

曲線使用共同起日的調整收盤價為 0%，每 N 個日報酬區間需要 N+1 筆完整日線。近期上市、缺日或異常來源會保留在表格中說明原因，不用較短區間代替。至少兩檔可用時才繪圖；表格列出共同起迄價格、百分比變化與有效／需要筆數。

計算完成會收合股票池，方便直接檢查結果；按「選擇其他標的」可再展開。變更期間、標的或工作區資料後，保留的結果會標示需要重新比較。詳細口徑見[比較方法](docs/candidate-comparison.md)。
